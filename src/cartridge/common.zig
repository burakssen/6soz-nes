pub const Mirroring = enum {
    single_screen_lower,
    single_screen_upper,
    horizontal,
    vertical,
    four_screen,
};

pub const TimingMode = enum {
    ntsc,
    pal,
    multiple,
    dendy,
};
